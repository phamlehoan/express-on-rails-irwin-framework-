export enum RestActions {
  Index = "index",
  Show = "show",
  New = "new",
  Create = "create",
  Edit = "edit",
  Update = "update",
  Destroy = "destroy",
}

export enum FlashType {
  Errors = 'errors',
  Info = 'info',
  Success = "success",
}
