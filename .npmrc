# Netlify zip-it-and-ship-it + esbuild sometimes calls readFile on paths that are
# directories under pnpm’s default symlink store → EISDIR. Hoisting matches what
# many Netlify + pnpm guides recommend for serverless bundling.
shamefully-hoist=true
